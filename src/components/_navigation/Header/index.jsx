import UserAccount from '../UserAccount';

const Header = () => {
  return (
    <section className="px-10 py-3 flex justify-end items-center">
      <UserAccount />
    </section>
  );
};

export default Header;
